# Synchronize_Files_Between_Servers
This script makes installation and configuration of directory synchronization that you specified during runtime.

Run the .sh file with 

```
chmod +x Synchronizer.sh
./Synchronizer.sh 
```

Enjoy...
